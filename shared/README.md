# Shared Contracts

Versioned, framework-neutral JSON Schemas live under `schemas/`. The QA specification is the source of truth for acceptance criteria, risks, and test strategy. Traceability records connect generated tests and execution evidence back to that specification.
